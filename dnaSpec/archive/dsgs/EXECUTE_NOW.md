# DNASPEC 立即执行命令

## 🚀 开始执行 - 阶段1: 环境确定性重建

### 立即执行的第一个命令
```bash
# 任务1.1.1: 删除所有现有依赖
rm -rf node_modules package-lock.json
npm cache clean --force
```

### 验证删除成功
```bash
# 检查是否删除成功
ls -la node_modules 2>/dev/null || echo "✅ node_modules 已删除"
ls -la package-lock.json 2>/dev/null || echo "✅ package-lock.json 已删除"
```

### 下一步命令 (在上述命令成功后)
```bash
# 任务1.1.2: 重新安装基础依赖
npm install
```

### 验证基础安装
```bash
# 验证基础依赖安装
npm list jest
npm list typescript
```

### 关键依赖安装
```bash
# 任务1.1.3: 安装精确版本的测试依赖
npm install --save-dev fast-check@3.0.0
npm install --save-dev @pact-foundation/pact@13.0.0
npm install --save-dev ts-node@10.9.2
```

### 最终验证
```bash
# 任务1.1.4: 验证安装成功
npm list fast-check && npm list @pact-foundation/pact
```

## 📋 快速检查清单

执行完每个命令后，在 `TASK_EXECUTION_CHECKLIST.md` 中标记完成状态：

1. [ ] 删除所有现有依赖
2. [ ] 重新安装基础依赖
3. [ ] 安装精确版本的测试依赖
4. [ ] 验证安装成功

## 🎯 当前目标

**立即目标**: 完成阶段1任务1.1.1
**预计时间**: 5-10分钟
**成功标准**: 
- ✅ node_modules 目录删除
- ✅ package-lock.json 文件删除
- ✅ npm缓存清理

## 🚨 如果遇到问题

### 权限问题
```bash
# Windows环境可能需要管理员权限
# 或者使用 sudo (Linux/Mac)
sudo rm -rf node_modules package-lock.json
```

### 文件被占用
```bash
# 关闭所有可能使用这些文件的进程
# 然后重试删除命令
```

### npm命令失败
```bash
# 检查npm是否正常工作
npm --version
npm config list
```

---

**立即执行**: 复制并运行第一个命令开始！