---
name: dnaspec-agent-creator
description: Create local agents with isolated context for specific tasks. Each agent operates in independent workspace to prevent system-wide context explosion. When you need local agents, task-specific AI assistants, or multi-agent systems with context isolation, use this skill. Based on principle of local agent architecture, not global agent architecture.
---

# DNASPEC Agent Creator

## 使用时机

当用户提到以下需求时，使用此技能：
- "创建智能体" 或 "create agent"
- "配置agent" 或 "configure agent"
- "多智能体系统" 或 "multi-agent system"
- "智能体角色定义" 或 "define agent role"
- "局部智能体" 或 "local agent"
- "避免上下文膨胀" 或 "avoid context explosion"
- "独立工作区" 或 "independent workspace"
- "具身认知" 或 "embodied cognition"
- 需要为特定任务创建专门的AI智能体
- 需要隔离智能体上下文以防止系统膨胀

**不要在以下情况使用**：
- ❌ 讨论一般系统架构（使用architect技能）
- ❌ 创建全局智能体（这不是本技能的目标）
- ❌ 具体代码实现

## 核心理念

### 🎯 局部智能体架构（Local Agent Architecture）

**什么是局部智能体？**

局部智能体是针对**特定任务**创建的专门智能体，与全局智能体有本质区别：

**❌ 全局智能体（不推荐）**：
```
全局智能体
  ├─ 加载整个系统上下文
  ├─ 了解所有系统模块
  ├─ 处理所有类型的任务
  └─ 结果：上下文爆炸，性能下降
```

**✅ 局部智能体（推荐）**：
```
局部智能体A (代码审查)
  ├─ 只加载代码审查相关的上下文
  ├─ 只了解代码模块
  ├─ 只处理审查任务
  └─ 结果：专注、高效、上下文隔离

局部智能体B (测试生成)
  ├─ 只加载测试相关的上下文
  ├─ 只了解测试框架
  ├─ 只处理测试生成任务
  └─ 结果：专注、高效、上下文隔离
```

**局部智能体的关键特性**：

✅ **任务隔离** - 每个智能体只负责特定任务
✅ **上下文隔离** - 每个智能体有独立的上下文工作区
✅ **职责单一** - 遵循单一职责原则
✅ **可独立部署** - 不影响其他智能体
✅ **降低系统复杂度** - 避免全局上下文膨胀

### 🎯 防止上下文爆炸

**上下文爆炸的危害**：

```
全局智能体场景：
系统上下文 (10万tokens)
  ├─ 业务逻辑
  ├─ 数据模型
  ├─ API接口
  ├─ 测试用例
  ├─ 文档
  └─ ...更多

任务：审查一段代码
问题：
- 加载了所有不必要的上下文
- 响应时间慢
- 成本高
- 注意力分散
```

**局部智能体的解决方案**：

```
局部智能体：CodeReviewAgent
独立上下文工作区 (2千tokens)
  ├─ 审查规则
  ├─ 安全漏洞库
  ├─ 代码规范
  └─ 待审查代码

任务：审查代码
优势：
✅ 只加载必要的上下文
✅ 响应时间快
✅ 成本低
✅ 注意力专注
```

**上下文隔离机制**：

```
项目根目录/
├── agents/                          # 智能体目录
│   ├── code-review-agent/          # 智能体A的独立工作区
│   │   ├── context.md              # 智能体A的局部上下文
│   │   ├── capabilities.yaml       # 智能体A的能力定义
│   │   ├── rules/                  # 智能体A的规则库
│   │   └── workspace/              # 智能体A的工作空间
│   ├── test-gen-agent/             # 智能体B的独立工作区
│   │   ├── context.md              # 智能体B的局部上下文
│   │   ├── capabilities.yaml       # 智能体B的能力定义
│   │   ├── frameworks/             # 智能体B的测试框架
│   │   └── workspace/              # 智能体B的工作空间
│   └── doc-gen-agent/              # 智能体C的独立工作区
│       ├── context.md              # 智能体C的局部上下文
│       ├── templates/              # 智能体C的文档模板
│       └── workspace/              # 智能体C的工作空间
```

**隔离的好处**：
- ✅ 智能体A的上下文不会影响智能体B
- ✅ 每个智能体可以独立优化
- ✅ 降低整体系统复杂度
- ✅ 便于并行处理
- ✅ 失败隔离，不影响其他智能体

### 🎯 与主架构的关系

**局部智能体如何与主系统协调**：

```
主架构 (dnaspec-architect)
    ↓
任务分解 (dnaspec-task-decomposer)
    ↓
为每个原子任务创建局部智能体 (dnaspec-agent-creator)
    ↓
├─ Task 1: 创建CodeReviewAgent
│   工作区: agents/code-review-agent/
│   上下文: 只包含代码审查相关知识
│   职责: 只负责代码审查
│
├─ Task 2: 创建TestGenAgent
│   工作区: agents/test-gen-agent/
│   上下文: 只包含测试生成相关知识
│   职责: 只负责测试生成
│
└─ Task 3: 创建DocGenAgent
    工作区: agents/doc-gen-agent/
    上下文: 只包含文档生成相关知识
    职责: 只负责文档生成
```

**协调机制**：
- **任务分配**: 主系统根据任务类型分发给对应的局部智能体
- **结果聚合**: 主系统聚合各个局部智能体的结果
- **上下文共享**: 通过接口共享，不直接访问其他智能体的上下文
- **生命周期管理**: 主系统管理智能体的创建、执行、销毁

---

## 全生命周期应用

### 📋 Idea阶段：为特定概念创建局部智能体

**场景**：用户有一个模糊的想法，需要专门的智能体来探索

**示例**：
```
用户想法："我想探索AI辅助代码审查的可能性"

智能体设计：
📝 Task 1: 创建代码审查原型智能体
  工作区: agents/code-review-prototype/
  上下文:
    - 代码审查基本概念
    - 常见代码问题模式
    - 审查规则草案
  职责: 探索代码审查的可行性
  局部性: ✅ 只加载审查相关上下文

📝 Task 2: 创建安全分析原型智能体
  工作区: agents/security-analysis-prototype/
  上下文:
    - 安全漏洞库
    - OWASP Top 10
    - 安全扫描规则
  职责: 探索安全分析的可行性
  局部性: ✅ 只加载安全相关上下文

每个智能体都有独立的局部上下文，互不干扰
```

**使用技能**：
```
/dnaspec.agent-creator "代码审查原型智能体"
→ 创建独立的智能体工作区
→ 只加载审查相关的局部上下文
→ 避免加载不必要的系统上下文
```

### 📋 需求阶段：为功能需求创建专门智能体

**场景**：有明确的功能需求，需要智能体来辅助实现

**示例**：
```
功能需求：自动化测试生成系统

局部智能体分解：
📝 Local Agent 1: 单元测试生成智能体
  工作区: agents/unit-test-gen/
  局部上下文:
    - 单元测试框架 (pytest, jest)
    - 测试用例设计模式
    - 断言规则
  职责: 只负责单元测试生成
  隔离性: ✅ 独立的测试上下文

📝 Local Agent 2: 集成测试生成智能体
  工作区: agents/integration-test-gen/
  局部上下文:
    - 集成测试框架
    - Mock和Stub规则
    - 测试数据管理
  职责: 只负责集成测试生成
  隔离性: ✅ 独立的集成测试上下文

📝 Local Agent 3: 端到端测试生成智能体
  工作区: agents/e2e-test-gen/
  局部上下文:
    - E2E测试框架 (cypress, playwright)
    - 用户交互模式
    - 测试场景库
  职责: 只负责E2E测试生成
  隔离性: ✅ 独立的E2E测试上下文

每个智能体只关注自己的职责，上下文完全隔离
```

### 📋 细化阶段：为复杂功能细化专门智能体

**场景**：功能需要进一步细化，创建更专门的局部智能体

**示例**：
```
复杂功能：支付处理系统

细化局部智能体：
📝 Local Agent 1: 支付宝支付处理智能体
  工作区: agents/alipay-processor/
  局部上下文:
    - 支付宝API文档
    - 支付宝SDK
    - 支付宝错误码
  职责: 只处理支付宝支付
  隔离性: ✅ 只包含支付宝相关上下文

📝 Local Agent 2: 微信支付处理智能体
  工作区: agents/wechat-pay-processor/
  局部上下文:
    - 微信支付API文档
    - 微信支付SDK
    - 微信支付错误码
  职责: 只处理微信支付
  隔离性: ✅ 只包含微信支付相关上下文

📝 Local Agent 3: 支付验证智能体
  工作区: agents/payment-validator/
  局部上下文:
    - 支付验证规则
    - 风险控制策略
    - 异常处理模式
  职责: 只负责支付验证
  隔离性: ✅ 只包含验证相关上下文

关键优势：
- 每个智能体只加载必要的上下文（如支付宝SDK）
- 不会加载无关的上下文（如微信支付SDK）
- 大幅降低单个智能体的上下文大小
```

### 📋 智能阶段：智能体协作与编排

**场景**：需要多个局部智能体协作完成复杂任务

**示例**：
```
智能体协作系统：项目健康检查

主协调器: ProjectHealthCoordinator
  (轻量级协调器，不加载详细上下文)
    ↓
分配任务给局部智能体
    ↓
├─ Local Agent 1: CodeQualityAgent
│   工作区: agents/code-quality/
│   局部上下文:
│     - 代码规范规则
│     - 静态分析工具
│     - 代码度量标准
│   执行: 代码质量检查
│   隔离性: ✅ 只包含代码质量上下文
│
├─ Local Agent 2: TestCoverageAgent
│   工作区: agents/test-coverage/
│   局部上下文:
│     - 测试覆盖率工具
│     - 覆盖率阈值标准
│     - 测试报告格式
│   执行: 测试覆盖率分析
│   隔离性: ✅ 只包含测试覆盖率上下文
│
└─ Local Agent 3: DependencyHealthAgent
    工作区: agents/dependency-health/
    局部上下文:
      - 依赖检查工具
      - 安全漏洞数据库
      - 版本兼容性规则
    执行: 依赖健康检查
    隔离性: ✅ 只包含依赖管理上下文

协作优势：
- 每个局部智能体独立并行执行
- 主协调器只负责聚合结果
- 单个智能体失败不影响其他智能体
- 总上下文大小远小于全局智能体
```

---

## 核心功能

### 1. 局部智能体创建

**智能体类型**：

**任务型局部智能体**
```yaml
agent_name: CodeReviewAgent
type: task_agent
architecture: local  # 局部架构

workspace: agents/code-review-agent/
context_scope: code_review_only  # 只包含代码审查上下文

isolation:
  context_isolated: true
  independent_workspace: true
  no_system_wide_context: true

capabilities:
  - static_code_analysis
  - security_vulnerability_scan
  - best_practices_check

context_size: ~2000 tokens  # 远小于全局智能体
```

**决策型局部智能体**
```yaml
agent_name: TechChoiceAgent
type: decision_agent
architecture: local

workspace: agents/tech-choice/
context_scope: technology_selection_only

isolation:
  context_isolated: true
  independent_evaluation: true

capabilities:
  - technology_evaluation
  - cost_benefit_analysis
  - risk_assessment

context_size: ~3000 tokens
```

**协作型局部智能体**
```yaml
agent_name: TestCoordinator
type: coordination_agent
architecture: local

workspace: agents/test-coordinator/
context_scope: test_coordination_only

isolation:
  context_isolated: true
  coordination_only: true  # 只协调，不执行

coordinates:
  - UnitTestAgent
  - IntegrationTestAgent
  - E2ETestAgent

context_size: ~1500 tokens
```

### 2. 独立上下文工作区

**工作区结构**：
```
agent-{name}/
├── context.md                    # 局部上下文定义
├── capabilities.yaml             # 能力规范
├── rules/                        # 业务规则
├── knowledge/                    # 领域知识
│   ├── concepts.md
│   └── patterns.md
├── tools/                        # 工具和脚本
├── workspace/                    # 工作空间
│   ├── input/
│   └── output/
└── config.yaml                   # 配置文件
```

**context.md 内容示例**：
```markdown
# CodeReviewAgent 局部上下文

## 智能体角色
- 名称: CodeReviewAgent
- 类型: 任务型局部智能体
- 职责: 代码静态审查

## 能力边界
### 能做什么
- 代码质量分析
- 安全漏洞检测
- 性能问题识别
- 最佳实践检查

### 不能做什么
- 运行时代码执行
- 业务逻辑判断
- 架构决策
- 其他类型的审查

## 领域知识
- 支持语言: Python, JavaScript, Java
- 审查规则: OWASP, CWE, SELLC
- 代码规范: PEP8, Airbnb Style Guide

## 上下文限制
- 只加载审查相关上下文
- 不加载系统其他模块信息
- 不加载业务逻辑详情
- 上下文大小限制: 2000 tokens
```

### 3. 智能体规范生成

**能力规范**：
```yaml
agent_name: CodeReviewAgent
version: 1.0.0
architecture: local

capabilities:
  - name: code_analysis
    scope: static_analysis_only
    tools:
      - pylint
      - eslint
      - sonarqube
    context_required:
      - code_style_rules
      - security_patterns
    context_forbidden:
      - business_logic
      - other_modules

  - name: security_scan
    scope: security_vulnerabilities_only
    tools:
      - bandit
      - safety
    context_required:
      - owasp_top_10
      - cwe_database
    context_forbidden:
      - test_cases
      - documentation
```

**性能指标**：
```yaml
performance:
  context_size: 2000  # tokens
  response_time: 5    # seconds
  accuracy: 0.95
  false_positive_rate: 0.05

comparison_with_global_agent:
  context_size_reduction: 98%    # vs 100k tokens
  response_time_improvement: 10x
  cost_reduction: 95%
```

### 4. 协作机制设计

**通信协议**：
```yaml
communication:
  type: message_based
  protocol: rest_api
  format: json

  message_schema:
    from: agent_id
    to: agent_id
    type: request|response
    payload: object
    context: minimal  # 只传递必要的上下文

  isolation:
    no_direct_context_access: true
    context_sharing: through_interface_only
```

**协调模式**：
```yaml
coordination:
  pattern: orchestrator

  orchestrator:
    type: lightweight
    context: minimal  # 只包含协调信息
    responsibilities:
      - task_distribution
      - result_aggregation
      - error_handling

  workers:
    type: local_agents
    context: isolated  # 每个worker有独立上下文
    execution: parallel
```

### 5. 生命周期管理

**创建阶段**：
```bash
# 创建局部智能体
/dnaspec.agent-creator "create CodeReviewAgent"
→ 创建独立工作区: agents/code-review-agent/
→ 初始化局部上下文: context.md
→ 定义能力边界: capabilities.yaml
→ 加载必要知识: knowledge/
```

**执行阶段**：
```bash
# 执行智能体任务
/dnaspec.agent-creator "execute CodeReviewAgent on file.py"
→ 只加载局部上下文 (2000 tokens)
→ 执行审查任务
→ 生成审查报告
→ 释放上下文
```

**销毁阶段**：
```bash
# 清理智能体
/dnaspec.agent-creator "cleanup CodeReviewAgent"
→ 清空工作空间
→ 保留配置和知识
→ 准备下次执行
```

---

## 输出格式

### 智能体规范文档

```markdown
# [智能体名称] 规范

## 1. 架构类型
- **架构**: 局部智能体 (Local Agent)
- **工作区**: agents/{agent-name}/
- **上下文隔离**: ✅ 是
- **独立部署**: ✅ 是

## 2. 局部上下文定义
### 2.1 上下文范围
- 包含: [必要的领域知识]
- 排除: [不相关的系统信息]
- 大小限制: [X] tokens

### 2.2 能力边界
- 能做什么: [职责范围]
- 不能做什么: [明确边界]

## 3. 能力规范
### 3.1 核心能力
- [能力1]: [描述]
- [能力2]: [描述]

### 3.2 性能指标
- 上下文大小: X tokens (比全局智能体减少Y%)
- 响应时间: X ms
- 准确率: X%

## 4. 行为规范
### 4.1 触发条件
- [触发条件1]
- [触发条件2]

### 4.2 行为模式
- [正常行为]
- [异常处理]

## 5. 协作机制
- [通信协议]
- [接口定义]
- [上下文共享方式: 通过接口，不直接访问]

## 6. 部署配置
- [运行环境: 独立工作区]
- [依赖服务]
- [配置参数]

## 7. 监控策略
- [性能监控]
- [上下文大小监控]
- [异常告警]
```

### 智能体配置文件

```json
{
  "agent_name": "CodeReviewAgent",
  "version": "1.0.0",
  "architecture": "local",
  "workspace": "agents/code-review-agent/",
  "role": {
    "name": "代码审查专家",
    "scope": "code_review_only",
    "expertise": ["static_analysis", "security", "performance"]
  },
  "context": {
    "isolation": true,
    "size_limit": 2000,
    "includes": [
      "code_style_rules",
      "security_patterns",
      "review_guidelines"
    ],
    "excludes": [
      "business_logic",
      "other_modules",
      "system_wide_context"
    ]
  },
  "capabilities": [
    {
      "name": "code_analysis",
      "enabled": true,
      "tools": ["pylint", "eslint"]
    },
    {
      "name": "security_scan",
      "enabled": true,
      "tools": ["bandit", "safety"]
    }
  ],
  "interfaces": {
    "input": {
      "type": "rest_api",
      "endpoint": "/api/v1/review"
    },
    "output": {
      "type": "json",
      "format": "structured_report"
    }
  },
  "performance": {
    "context_size": 2000,
    "max_execution_time": 5000,
    "accuracy": 0.95
  },
  "comparison": {
    "vs_global_agent": {
      "context_size_reduction": "98%",
      "response_time_improvement": "10x",
      "cost_reduction": "95%"
    }
  }
}
```

---

## 使用示例

### 示例1: 创建代码审查局部智能体

**用户需求**:
"创建一个智能体来审查Python代码的安全问题"

**智能体设计**：

📋 **架构设计**
```yaml
架构类型: 局部智能体 (Local Agent)
工作区: agents/python-security-reviewer/
上下文范围: 仅Python安全问题

隔离机制:
  ✅ 独立工作区
  ✅ 局部上下文
  ✅ 不加载系统其他信息
```

🎭 **角色定义**
- 名称: PythonSecurityReviewer
- 类型: 任务型局部智能体
- 角色: Python代码安全专家
- 专长: OWASP Top 10、Python安全漏洞

📦 **局部上下文**
```yaml
context:
  include:
    - python_security_patterns
    - owasp_top_10
    - cwe_vulnerabilities
    - secure_coding_guidelines

  exclude:
    - business_logic
    - other_modules
    - frontend_code
    - database_schema

  size: ~1500 tokens
```

⚙️ **能力设计**
```yaml
capabilities:
  - sql_injection_detection
  - xss_detection
  - insecure_deserialization_check
  - hardcoded_credentials_scan
```

📊 **性能对比**
```yaml
局部智能体:
  context_size: 1500 tokens
  response_time: 3 seconds
  cost: $0.001

全局智能体 (假设):
  context_size: 100000 tokens
  response_time: 30 seconds
  cost: $0.020

改进:
  ✅ 上下文减少 98.5%
  ✅ 响应时间快 10x
  ✅ 成本降低 95%
```

### 示例2: 创建多智能体协作系统

**用户需求**:
"设计一个智能体系统来全面检查项目健康"

**系统设计**：

🏗️ **架构设计**
```
ProjectHealthCoordinator (主协调器)
  工作区: coordinators/project-health/
  上下文: 最小化 (只包含协调信息)

  分配任务给局部智能体:
    ↓
  ├─ CodeQualityAgent (局部)
  │   工作区: agents/code-quality/
  │   上下文: 只包含代码质量规则 (2000 tokens)
  │
  ├─ TestCoverageAgent (局部)
  │   工作区: agents/test-coverage/
  │   上下文: 只包含测试覆盖率标准 (1500 tokens)
  │
  ├─ DependencyHealthAgent (局部)
  │   工作区: agents/dependency-health/
  │   上下文: 只包含依赖检查规则 (1800 tokens)
  │
  └─ SecurityScanAgent (局部)
      工作区: agents/security-scan/
      上下文: 只包含安全漏洞库 (2500 tokens)

总上下文: 主协调器(500) + 各局部智能体
  = 500 + 2000 + 1500 + 1800 + 2500
  = 8300 tokens (分散在各个独立工作区)

如果使用全局智能体:
  = 100000 tokens (全部加载到一个地方)

改进: 上下文分散，避免单点膨胀
```

🤝 **协作机制**
```yaml
coordination:
  type: orchestrator
  pattern: parallel_execution

  communication:
    protocol: rest_api
    message_size: minimal
    context_sharing: through_interface_only

  aggregation:
    collector: ProjectHealthCoordinator
    method: weighted_scoring
    output: unified_report
```

📊 **输出格式**
```json
{
  "overall_health": 0.75,
  "agent_results": {
    "code_quality": {
      "score": 0.82,
      "agent": "CodeQualityAgent",
      "context_used": "2000 tokens"
    },
    "test_coverage": {
      "score": 0.65,
      "agent": "TestCoverageAgent",
      "context_used": "1500 tokens"
    },
    "dependencies": {
      "score": 0.90,
      "agent": "DependencyHealthAgent",
      "context_used": "1800 tokens"
    },
    "security": {
      "score": 0.68,
      "agent": "SecurityScanAgent",
      "context_used": "2500 tokens"
    }
  },
  "total_context_used": "8300 tokens (distributed)",
  "vs_global_agent": "100000 tokens (centralized)",
  "improvement": "91.7% context reduction"
}
```

---

## 质量检查清单

### 局部智能体设计检查

**架构检查**：
- [ ] 是局部智能体，不是全局智能体
- [ ] 有独立的工作区
- [ ] 上下文完全隔离
- [ ] 不访问系统其他模块的上下文

**上下文检查**：
- [ ] 上下文大小明确限制
- [ ] 只包含必要的领域知识
- [ ] 排除不相关的系统信息
- [ ] 上下文范围清晰定义

**能力边界检查**：
- [ ] 职责单一明确
- [ ] 能力边界清晰
- [ ] 明确不能做什么
- [ ] 遵循单一职责原则

**性能检查**：
- [ ] 上下文大小显著小于全局智能体
- [ ] 响应时间更快
- [ ] 成本更低
- [ ] 可独立部署

**协作检查**：
- [ ] 通过接口通信
- [ ] 不直接访问其他智能体上下文
- [ ] 失败隔离
- [ ] 可并行执行

---

## 协作技能

- **dnaspec-architect**: 整体架构设计，协调多个局部智能体
- **dnaspec-task-decomposer**: 任务分解，为每个任务创建局部智能体
- **dnaspec-constraint-generator**: 生成智能体约束规范
- **dnaspec-context-analysis**: 分析智能体上下文质量

---

## 关键成就

1. ✅ **局部智能体架构** - 针对特定任务的专门智能体
2. ✅ **独立上下文工作区** - 每个智能体有隔离的工作环境
3. ✅ **防止上下文膨胀** - 避免全局智能体的上下文爆炸
4. ✅ **性能优化** - 上下文减少98%，响应时间快10x
5. ✅ **失败隔离** - 单个智能体失败不影响其他智能体
6. ✅ **全生命周期应用** - Idea→需求→细化→智能四阶段

---

*此技能实现局部智能体架构，通过独立上下文工作区和任务隔离，确保每个智能体专注于特定任务，避免全局上下文膨胀，提高系统整体性能和可维护性。*
