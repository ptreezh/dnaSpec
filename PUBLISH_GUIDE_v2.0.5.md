# DNASPEC v2.0.5 发布指南

**准备日期**: 2025-12-26
**版本**: 2.0.5
**状态**: ✅ 包已构建，待发布

---

## ✅ 已完成的准备工作

### 1. 版本更新
- ✅ 更新根目录 `package.json` 版本至 2.0.5
- ✅ 更新 `package/package.json` 版本至 2.0.5
- ✅ 修复 `bin/dnaspec-cli.js` 版本读取语法
- ✅ 所有文档版本号已更新至 v2.0.5

### 2. 包结构准备
- ✅ 复制 `bin/` 目录到 `package/bin/`
- ✅ 复制 `src/` 目录到 `package/src/`
- ✅ 复制 `README_MAIN.md` 到 `package/README.md`
- ✅ 包结构验证完成

### 3. 包构建
- ✅ 成功构建 `dnaspec-2.0.5.tgz`
- ✅ 包大小: 2.0 MB (解压后: 4.2 MB)
- ✅ 包含文件数: 339

### 4. 本地测试
- ✅ 安装测试通过
- ✅ 版本号显示正确 (2.0.5)
- ✅ 命令功能正常
- ✅ 所有技能列表正确显示

---

## 📦 包信息

```bash
名称: dnaspec
版本: 2.0.5
文件名: dnaspec-2.0.5.tgz
位置: /d/DAIP/dnaSpec/package/
大小: 2.0 MB
文件数: 339
SHASUM: 2873411007bbb72e030e3286a4e3cb8a0fa84bb4
```

---

## 🚀 发布步骤

### 步骤1: 登录NPM（如果未登录）

```bash
npm login
```

按提示输入：
- 用户名
- 密码
- 邮箱

**或使用Token**:
```bash
npm token create
# 复制生成的token
npm config set //registry.npmjs.org/:_authToken <your-token>
```

### 步骤2: 验证当前状态

```bash
# 查看当前登录用户
npm whoami

# 查看包信息（如果已存在）
npm view dnaspec
```

### 步骤3: 进入package目录

```bash
cd /d/DAIP/dnaSpec/package
```

### 步骤4: 发布到NPM

```bash
npm publish ./dnaspec-2.0.5.tgz
```

**预期输出**:
```
npm notice
npm notice 📦 dnaspec@2.0.5
npm notice === Tarball Contents ===
npm notice ... (文件列表)
npm notice === Tarball Details ===
npm notice name: dnaspec
npm notice version: 2.0.5
npm notice package size: 2.0 MB
npm notice unpacked size: 4.2 MB
npm notice shasum: 2873411007bbb72e030e3286a4e3cb8a0fa84bb4
npm notice integrity: sha512-...
npm notice total files: 339
npm notice
+ dnaspec@2.0.5
```

### 步骤5: 验证发布

```bash
# 验证包已发布
npm view dnaspec

# 查看版本信息
npm view dnaspec version

# 测试全局安装（可选）
npm install -g dnaspec@latest
dnaspec --version  # 应该显示 2.0.5
```

---

## 📋 v2.0.5 更新内容

### 🎉 重要修复

**修复了所有文档和CLI工具中的错误命令格式**

### ✅ 修复内容

#### CLI工具
- 修复 `bin/dnaspec-cli.js` 显示正确的使用方式
- 当用户使用 `dnaspec slash` 时显示清晰警告
- `dnaspec list` 显示正确的Slash命令格式

#### 文档
- **README_MAIN.md** - 所有技能示例改为正确的Slash命令格式
- **USER_MANUAL.md** - 完整修复用户手册
- **DEPLOYMENT_GUIDE.md** - 更新部署指南
- **TROUBLESHOOTING.md** - 修复所有故障排除示例

#### 新增文档
- **CORRECT_USAGE_GUIDE.md** - 完整的正确使用指南
- **CHANGELOG.md** - 版本更新日志
- **COMMAND_FORMAT_CLEANUP_REPORT.md** - 清理工作报告
- **FINAL_CLEANUP_REPORT.md** - 最终完成报告

### ⚠️ 重要变更

**移除错误的命令格式**:
- ❌ `dnaspec slash <技能名>` - 不再支持
- ❌ `dnaspec agent-creator "任务"` - 不再支持

**正确的使用方式**:
- ✅ CLI: `dnaspec list`, `dnaspec deploy`, `dnaspec validate`
- ✅ AI编辑器: `/dnaspec.agent-creator`, `/dnaspec.task-decomposer`

---

## 🔍 发布后验证清单

- [ ] NPM页面显示正确的版本号 (2.0.5)
- [ ] 可以通过 `npm install -g dnaspec@latest` 安装
- [ ] `dnaspec --version` 显示 v2.0.5
- [ ] `dnaspec list` 显示正确的技能列表
- [ ] `dnaspec slash test` 显示正确的警告
- [ ] 文档链接正确指向 GitHub

---

## 📝 发布后任务

### 1. 更新GitHub Release

1. 访问: https://github.com/your-repo/dnaspec/releases
2. 点击 "Draft a new release"
3. 标题: `v2.0.5 - 重要修复：文档和CLI命令格式`
4. 内容: 使用 CHANGELOG.md 中的 [2.0.5] 部分
5. 上传 `dnaspec-2.0.5.tgz` 文件

### 2. 通知用户

**渠道**:
- GitHub Release Notes
- README更新
- 社区公告（如有）

**内容模板**:
```
🎉 DNASPEC v2.0.5 发布

重要修复：
✅ 修复了所有文档中的错误命令格式
✅ CLI工具现在显示正确的使用提示
✅ 创建了完整的使用指南

升级方式：
npm update -g dnaspec
# 或
npm install -g dnaspec@latest
```

### 3. 更新文档网站（如有）

确保文档站点反映最新版本的变化。

---

## ⚠️ 注意事项

### 发布权限

确保您有 `dnaspec` 包的发布权限：
- 如果是您自己的包：直接发布
- 如果是组织包：需要组织成员权限

### OTP (One-Time Password)

如果启用了双因素认证：
- 发布时可能需要输入OTP
- 检查您的认证器应用获取OTP

### 包名冲突

如果包名已被占用：
- 需要使用不同的包名
- 或联系原包所有者

---

## 🔄 回滚计划（如需要）

如果发现问题需要回滚：

```bash
# 取消发布（24小时内）
npm unpublish dnaspec@2.0.5 --force

# 重新发布旧版本
npm publish ./dnaspec-2.0.4.tgz
```

**注意**: `npm unpublish` 有时间限制（通常24-48小时）

---

## 📊 发布统计

| 项目 | 值 |
|------|-----|
| 版本号 | 2.0.5 |
| 包大小 | 2.0 MB |
| 解压大小 | 4.2 MB |
| 文件数 | 339 |
| 修改行数 | ~825 |
| 新增文档 | 4个 |

---

## ✅ 完成后

发布成功后，请验证：

```bash
# 1. 清理缓存并重新安装
npm cache clean --force
npm install -g dnaspec@latest

# 2. 验证版本
dnaspec --version
# 应该输出: 2.0.5

# 3. 验证功能
dnaspec list
dnaspec tips

# 4. 测试错误命令
dnaspec slash test
# 应该显示正确的警告
```

---

## 🎯 总结

所有准备工作已完成：

✅ 版本更新完成
✅ 包结构准备完成
✅ 包构建完成
✅ 本地测试通过
✅ 文档更新完成

**下一步**: 执行发布步骤中的 `npm publish` 命令

---

**准备状态**: ✅ 就绪发布
**准备时间**: 2025-12-26
