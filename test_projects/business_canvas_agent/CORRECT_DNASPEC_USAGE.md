# DNASPEC 正确使用方式 - 技能部署和管理

## 🎯 核心理念

**DNASPEC 不是技能运行器，而是技能部署和管理工具！**

```
┌─────────────────┐
│   DNASPEC       │  技能管理器
│  (安装/部署)    │
└────────┬────────┘
         │
         │ deploy
         ↓
┌─────────────────┐
│   AI CLI 工具   │  技能运行环境
│  (iflow/claude) │
└────────┬────────┘
         │
         │ 在此环境中运行
         ↓
┌─────────────────┐
│  DNASPEC 技能   │  实际执行
└─────────────────┘
```

---

## 📋 两种部署模式

### 模式 1: Stigmergy 全局部署 ✅ 推荐

**特点**：
- 全局协作部署
- 基于当前工作目录的安全上下文
- 支持 Stigmergy 跨 AI 工具协作
- 自动安全验证

**命令**：
```bash
# 自动检测并使用 Stigmergy 模式
dnaspec deploy

# 强制使用 Stigmergy 模式
dnaspec deploy --force-stigmergy

# 验证部署
dnaspec deploy --verify
```

**部署位置**：
```
~/.stigmergy/hooks/        # 全局钩子
.dnaspec/cli_extensions/   # CLI 扩展
```

**适用场景**：
- 多个 AI 工具需要共享技能
- 跨项目使用技能
- 需要 Stigmergy 协作功能

---

### 模式 2: 项目级独立部署

**特点**：
- 项目目录隔离
- 只能访问当前项目
- 独立的安全上下文
- 更严格的权限控制

**命令**：
```bash
# 强制使用项目级模式
dnaspec deploy --force-project

# 部署到特定项目
cd /path/to/project
dnaspec deploy --force-project
```

**部署位置**：
```
<project>/.dnaspec/slash_commands/   # 项目级命令
```

**适用场景**：
- 单一项目使用
- 需要严格的隔离
- 不需要全局协作

---

## 🚀 完整使用流程

### 步骤 1: 检查部署状态

```bash
cd D:/DAIP/dnaSpec
dnaspec deploy --list
```

**输出示例**：
```
🚀 DNASPEC CLI Extension Deployment Status:
  📍 Project Root: D:\DAIP\dnaSpec
  🔧 Deployment Mode: stigmergy
  📋 Stigmergy Available: True
  🛠️  Supported AI Tools: claude, cursor, vscode, iflow, ...
```

### 步骤 2: 部署技能

```bash
# 使用 Stigmergy 全局部署（推荐）
dnaspec deploy

# 或强制项目级部署
dnaspec deploy --force-project

# 验证部署
dnaspec deploy --verify
```

### 步骤 3: 在 AI CLI 中使用技能

#### 方式 A: 在 iflow 中使用

```bash
# 启动 iflow
iflow

# 在 iflow 中直接使用技能（不是 dnaspec 命令）
/请扮演系统架构师，为商业画布分析智能体设计架构
```

**注意**：
- ❌ 不要使用：`dnaspec slash system-architect`
- ✅ 正确方式：在 iflow 中直接对话

#### 方式 B: 在 Claude Code 中使用

```bash
# 启动 claude
claude

# 直接使用技能
/请作为系统架构师设计...
```

#### 方式 C: 在 Cursor 中使用

```bash
# 在 Cursor 中通过命令面板使用
# Cmd+Shift+P → DNASPEC Skills
```

---

## 🔍 查看可用技能

```bash
# 列出所有 DNASPEC 技能
dnaspec list
```

**输出**：
```
🛠️  可用的DNASPEC技能:

  • system-architect      - 系统架构技能
  • task-decomposer       - 任务分解技能
  • constraint-generator  - 约束生成技能
  • agent-creator         - 智能体创建技能
  • context-analysis      - 上下文分析技能
  • context-optimization  - 上下文优化技能
  ...
```

---

## ⚠️ 常见错误

### ❌ 错误 1: 使用 dnaspec slash 命令

```bash
# 错误！这是错误的命令格式
dnaspec slash system-architect "..."
```

**原因**：
- DNASPEC 不执行技能
- `slash` 命令不是正确的使用方式

### ❌ 错误 2: 期望 DNASPEC 直接运行技能

```bash
# 错误！DNASPEC 只负责部署
dnaspec exec system-architect "..."
```

**正确理解**：
- DNASPEC = 技能包管理器（类似 npm）
- AI CLI = 技能运行环境（类似 Node.js）

---

## ✅ 正确的使用方式总结

### DNASPEC 的作用

```bash
# 1. 部署技能到 AI CLI
dnaspec deploy --force-stigmergy

# 2. 查看可用技能
dnaspec list

# 3. 验证部署
dnaspec deploy --verify

# 4. 集成到特定平台
dnaspec integrate --platform iflow
```

### AI CLI 的作用

```bash
# 启动 AI CLI
iflow

# 在 AI CLI 中直接使用技能
/请扮演系统架构师，设计...
```

---

## 📊 两种模式对比

| 特性 | Stigmergy 全局部署 | 项目级独立部署 |
|------|-------------------|----------------|
| **部署范围** | 全局 | 当前项目 |
| **安全上下文** | 基于当前工作目录 | 项目目录隔离 |
| **跨项目共享** | ✅ 是 | ❌ 否 |
| **Stigmergy 协作** | ✅ 支持 | ❌ 不支持 |
| **权限控制** | 标准控制 | 严格隔离 |
| **部署位置** | `~/.stigmergy/hooks/` | `<project>/.dnaspec/` |
| **适用场景** | 多项目协作 | 单项目专用 |

---

## 🎯 实际示例

### 示例：商业画布分析智能体

#### 1. 部署技能

```bash
cd D:/DAIP/dnaSpec/test_projects/business_canvas_agent

# 使用 Stigmergy 全局部署
dnaspec deploy --force-stigmergy
```

**输出**：
```
🔒 DNASPEC Secure Deployment Manager
📍 Project Root: D:\DAIP\dnaSpec\test_projects\business_canvas_agent
🔧 Deployment Mode: stigmergy
🛡️  Security Context: standard
✅ Deployment completed
```

#### 2. 在 AI CLI 中使用

```bash
# 启动 iflow
iflow

# 在 iflow 中直接对话
/请作为系统架构师，为商业画布分析智能体设计系统架构

# 然后继续对话
/请作为任务分解专家，将这个项目分解为开发任务

/请作为约束生成专家，生成业务规则和安全约束
```

---

## 📝 重要提示

### DNASPEC 是什么？

✅ **DNASPEC 是**：
- 技能包管理器
- 技能部署工具
- 技能生命周期管理器

❌ **DNASPEC 不是**：
- 技能执行器
- AI CLI 替代品
- 直接运行技能的工具

### 技能在哪里运行？

✅ **技能在 AI CLI 中运行**：
- iflow
- claude
- cursor
- 其他 AI CLI 工具

### 正确的工作流程

```
1. 开发技能 (Python)
      ↓
2. 打包技能 (DNASPEC)
      ↓
3. 部署技能 (dnaspec deploy)
      ↓
4. 在 AI CLI 中使用 (iflow/claude/...)
```

---

## 🔧 部署命令速查

```bash
# 查看部署状态
dnaspec deploy --list

# Stigmergy 全局部署（推荐）
dnaspec deploy
dnaspec deploy --force-stigmergy

# 项目级部署
dnaspec deploy --force-project

# 验证部署
dnaspec deploy --verify

# 查看可用技能
dnaspec list

# 集成到平台
dnaspec integrate --platform iflow
dnaspec integrate --list
```

---

## 📚 相关文档

- `STIGMERGY.md` - Stigmergy 协作系统说明
- `src/dna_spec_kit_integration/core/secure_deployment_manager.py` - 部署管理器实现

---

**最后更新**: 2025-12-25
**版本**: 1.0
**状态**: ✅ 正确理解并已修正
