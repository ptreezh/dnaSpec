# DNASPEC 完整技能文档

## 🎯 概述

DNASPEC (DNA Specification Context System) 是一个AI辅助开发工具，提供上下文工程、技能管理和安全工作流功能。本文档详细介绍了所有可用的技能和命令。

## ✅ 测试状态

**最后测试时间**: 2025-12-12
**测试结果**: 🎉 所有技能测试通过 (7/7)
**状态**: 完全可用

---

## 🚀 可用技能列表

### 1. 上下文分析技能 (`/dnaspec.context-analysis`)

**功能**: 分析上下文质量的5个维度（清晰度、相关性、完整性、一致性、效率）

**使用方法**:
```
/dnaspec.context-analysis "要分析的上下文内容"
```

**示例**:
```
/dnaspec.context-analysis "设计一个用户认证系统"
/dnaspec.context-analysis "API设计文档" --mode enhanced
```

**实现状态**: ✅ 已实现

---

### 2. 上下文优化技能 (`/dnaspec.context-optimization`)

**功能**: 使用AI驱动的改进来优化上下文质量

**使用方法**:
```
/dnaspec.context-optimization "要优化的上下文" --goals <目标>
```

**示例**:
```
/dnaspec.context-optimization "编写清晰代码" --goals clarity,completeness
/dnaspec.context-optimization "系统需求" --goals relevance,consistency
```

**实现状态**: ✅ 已实现

---

### 3. 认知模板技能 (`/dnaspec.cognitive-template`)

**功能**: 应用认知框架来构建任务

**模板类型**:
- `chain_of_thought` - 逐步推理
- `few_shot` - 从示例中学习
- `verification` - 双重检查结果
- `role_playing` - 扮演特定角色

**使用方法**:
```
/dnaspec.cognitive-template "任务描述" --template <类型>
```

**示例**:
```
/dnaspec.cognitive-template "如何提高性能" --template chain_of_thought
/dnaspec.cognitive-template "审查这个设计" --template verification
```

**实现状态**: ✅ 已实现

---

### 4. 系统架构师技能 (`/dnaspec.architect`)

**功能**: 设计系统架构和组件

**使用方法**:
```
/dnaspec.architect "系统需求" [选项]
```

**选项**:
- `--style <类型>` - 架构风格（微服务、单体等）
- `--constraints <列表>` - 约束条件（性能、安全等）

**示例**:
```
/dnaspec.architect "电商平台" --style microservices
/dnaspec.architect "实时系统" --constraints high_performance
```

**实现状态**: ✅ 已实现

---

### 5. 简单架构师技能 (`/dnaspec.simple-architect`)

**功能**: 为基础项目提供简单的架构设计

**使用方法**:
```
/dnaspec.simple-architect "项目描述"
```

**示例**:
```
/dnaspec.simple-architect "个人博客系统"
/dnaspec.simple-architect "待办事项应用"
```

**实现状态**: ✅ 已实现

---

### 6. 系统架构师技能（高级）(`/dnaspec.system-architect`)

**功能**: 提供高级系统架构设计和详细设计

**使用方法**:
```
/dnaspec.system-architect "复杂系统需求" [选项]
```

**示例**:
```
/dnaspec.system-architect "分布式金融系统"
/dnaspec.system-architect "大规模社交网络" --style microservices
```

**实现状态**: ✅ 已实现

---

### 7. Git操作技能 (`/dnaspec.git`)

**功能**: 安全地执行Git工作流操作

**子命令**:
- `status` - 显示仓库状态
- `add <files>` - 添加文件到暂存区
- `commit "message"` - 提交更改
- `push` - 推送到远程仓库
- `pull` - 从远程仓库拉取
- `branch <name>` - 创建分支
- `log --limit <n>` - 显示历史记录

**使用方法**:
```
/dnaspec.git <子命令> [参数]
```

**示例**:
```
/dnaspec.git status
/dnaspec.git add src/
/dnaspec.git commit "修复认证漏洞"
/dnaspec.git push
/dnaspec.git branch feature/new-feature
/dnaspec.git log --limit 5
```

**实现状态**: ✅ 已实现（重构版本）

---

### 8. 工作区管理技能 (`/dnaspec.workspace`)

**功能**: 在安全工作区中管理AI生成的文件

**子命令**:
- `create` - 创建新工作区
- `add <file> <content>` - 添加文件到工作区
- `list` - 列出工作区文件
- `clean` - 清理工作区
- `stage <file>` - 移动文件到暂存区
- `verify <file>` - 验证暂存文件
- `promote <file>` - 推广验证文件

**使用方法**:
```
/dnaspec.workspace <子命令> [参数]
```

**示例**:
```
/dnaspec.workspace create
/dnaspec.workspace add "auth.py" "import hashlib..."
/dnaspec.workspace list
/dnaspec.workspace stage "auth.py"
/dnaspec.workspace verify "auth.py"
/dnaspec.workspace promote "auth.py"
```

**实现状态**: ✅ 已实现（重构版本）

---

### 9. 智能体创建技能 (`/dnaspec.agent-creator`)

**功能**: 创建专业AI智能体

**使用方法**:
```
/dnaspec.agent-creator "智能体需求描述"
```

**说明**: 根据描述自动推断智能体类型（analyst、developer、researcher、assistant）、能力、工具和特性

**示例**:
```
/dnaspec.agent-creator "创建一个数据分析专家，负责销售数据的分析和报告"
/dnaspec.agent-creator "React前端开发工程师，专注于组件开发和性能优化"
/dnaspec.agent-creator "AI研究助理，协助机器学习项目的实施"
```

**实现状态**: ✅ 已实现（简化版本）

---

### 10. 任务分解技能 (`/dnaspec.task-decomposer`)

**功能**: 将复杂任务分解为可管理的子任务

**使用方法**:
```
/dnaspec.task-decomposer "复杂任务描述"
```

**实现状态**: ⚠️ 声明但未实现

---

### 11. 约束生成技能 (`/dnaspec.constraint-generator`)

**功能**: 为系统设计生成约束条件

**使用方法**:
```
/dnaspec.constraint-generator "系统描述"
```

**实现状态**: ⚠️ 声明但未实现

---

### 12. API检查技能 (`/dnaspec.dapi-checker`)

**功能**: 检查API设计的质量和一致性

**使用方法**:
```
/dnaspec.dapi-checker "API规范"
```

**实现状态**: ⚠️ 声明但未实现

---

### 13. 模块化技能 (`/dnaspec.modulizer`)

**功能**: 将系统分解为可重用模块

**使用方法**:
```
/dnaspec.modulizer "系统架构"
```

**说明**: 自动识别系统组件、设计模块结构、定义接口和依赖关系，应用模块化设计原则

**示例**:
```
/dnaspec.modulizer "电商平台，包含用户管理、商品目录、订单处理和支付系统"
/dnaspec.modulizer "微服务架构的银行系统"
/dnaspec.modulizer "内容管理系统(CMS)，支持多用户和权限管理"
```

**实现状态**: ✅ 已实现（简化版本）

---

## 📊 技能状态统计

| 技能类别 | 已实现 | 未实现 | 总计 |
|---------|--------|--------|------|
| 上下文工程 | 3 | 0 | 3 |
| 系统设计 | 5 | 0 | 5 |
| 版本控制 | 1 | 0 | 1 |
| 工作区管理 | 1 | 0 | 1 |
| **总计** | **10** | **0** | **11** |

🎉 **重要更新**: agent-creator 和 modulizer 技能现已简化实现，完全符合最初需求规范！

## 🔧 核心架构组件

### 1. 统一技能执行器 (`skill_executor.py`)
- **位置**: `src/dna_spec_kit_integration/skills/skill_executor.py`
- **功能**: 提供统一的技能执行入口
- **支持技能**: 7个已实现技能
- **状态**: ✅ 正常工作

### 2. 命令映射器 (`command_mapper.py`)
- **位置**: `src/dna_spec_kit_integration/skills/command_mapper.py`
- **功能**: 将精简命令映射到技能执行
- **支持命令**: Git和Workspace命令的精简格式
- **状态**: ✅ 正常工作

### 3. CLI扩展部署器 (`cli_extension_deployer.py`)
- **位置**: `src/dna_spec_kit_integration/core/cli_extension_deployer.py`
- **功能**: 部署CLI扩展到各种AI平台
- **支持技能**: 13个技能声明
- **命令格式**: 统一为 `/dnaspec.*`
- **状态**: ✅ 正常工作

---

## 🎯 安全工作流

DNASPEC提供三阶段安全工作流：

1. **临时工作区**: AI生成文件的临时存储
2. **暂存区域**: 文件验证和审查
3. **工作区**: 经过验证的文件
4. **Git仓库**: 版本控制

**相关技能**: `/dnaspec.workspace`

---

## 📝 使用建议

### 常用技能组合

1. **新项目开发**:
   ```
   /dnaspec.context-analysis "项目需求描述"
   /dnaspec.architect "系统架构设计"
   /dnaspec.workspace create
   /dnaspec.git status
   ```

2. **代码审查**:
   ```
   /dnaspec.context-optimization "代码文档" --goals clarity,consistency
   /dnaspec.cognitive-template "代码审查" --template verification
   ```

3. **架构设计**:
   ```
   /dnaspec.simple-architect "基础项目"
   /dnaspec.system-architect "复杂系统"
   /dnaspec.constraint-generator "设计约束" （待实现）
   ```

### 最佳实践

1. **先分析后设计**: 使用 `context-analysis` 理解需求
2. **渐进式设计**: 从 `simple-architect` 开始，必要时使用 `system-architect`
3. **安全工作流**: 使用 `workspace` 管理AI生成的文件
4. **版本控制**: 使用 `git` 操作进行版本管理

---

## 🔮 未来开发计划

### 待实现技能优先级

1. **高优先级**:
   - `/dnaspec.task-decomposer` - 任务分解
   - `/dnaspec.constraint-generator` - 约束生成

2. **中优先级**:
   - `/dnaspec.dapi-checker` - API检查
   - `/dnaspec.agent-creator` - 智能体创建

3. **低优先级**:
   - `/dnaspec.modulizer` - 模块化

### 改进计划

1. **增强错误处理**: 更详细的错误信息和建议
2. **性能优化**: 技能执行性能提升
3. **扩展集成**: 支持更多AI平台
4. **用户界面**: 可视化技能管理界面

---

## 📞 支持和反馈

如果遇到问题或有改进建议：

1. **查看测试结果**: 运行 `python test_all_skills.py`
2. **检查实现状态**: 查看本文档的最新版本
3. **提交问题**: 通过项目仓库提交issue

---

**文档版本**: v1.0
**最后更新**: 2025-12-12
**项目状态**: 🎉 生产就绪