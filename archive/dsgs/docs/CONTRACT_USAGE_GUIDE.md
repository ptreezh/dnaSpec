# DSGS契约管理模块使用指南

## 🚀 快速开始

### 1. 系统要求
- Node.js >= 18.0.0
- TypeScript >= 5.0.0
- npm 或 yarn 包管理器

### 2. 安装依赖
```bash
npm install
```

### 3. 生成初始契约
```bash
npm run contract:generate
```

### 4. 验证契约
```bash
npm run contract:validate
```

### 5. 生成文档
```bash
npm run contract:docs
```

## 📋 日常使用流程

### 开发阶段
1. **修改API代码**
2. **更新契约** - `npm run contract:generate`
3. **验证契约** - `npm run contract:validate`
4. **提交代码**

### 发布阶段
1. **版本检查** - 确保契约版本正确
2. **兼容性检查** - 验证向后兼容性
3. **生成文档** - `npm run contract:docs`
4. **发布版本**

## 🔧 配置说明

### 契约生成配置
```json
{
  "generation": {
    "sourcePaths": ["./src/api", "./src/controllers", "./src/dtos"],
    "outputPath": "./dist/contract.yaml",
    "format": "openapi",
    "includePrivate": false,
    "includeExamples": true,
    "validate": true,
    "version": "2.0.0"
  }
}
```

### 验证配置
```json
{
  "validation": {
    "level": "normal",
    "implementationPath": "./src",
    "autoFix": false
  }
}
```

### 集成配置
```json
{
  "integration": {
    "enabled": true,
    "autoGenerate": true,
    "autoValidate": true,
    "generateOnBuild": true,
    "validateOnTest": true
  }
}
```

## 📖 最佳实践

### 1. API设计原则
- **RESTful设计** - 遵循REST API设计原则
- **一致性** - 保持API命名和结构的一致性
- **版本控制** - 使用语义化版本控制
- **文档完整** - 确保API文档完整和准确

### 2. 契约管理原则
- **契约优先** - 先定义契约，再实现API
- **自动化** - 使用自动化工具生成和维护契约
- **验证** - 定期验证契约的正确性
- **版本管理** - 妥善管理契约版本

### 3. 开发流程
```
1. 设计API → 2. 编写契约 → 3. 实现代码 → 4. 验证契约 → 5. 提交代码
```

### 4. 团队协作
- **代码审查** - 包含契约变更的审查
- **文档同步** - 确保文档与契约同步
- **沟通机制** - 建立契约变更的沟通机制

## 🛠️ 故障排除

### 常见问题

#### 1. 契约生成失败
```bash
# 检查源码路径是否正确
npm run contract:generate --source ./src

# 检查输出目录权限
mkdir -p ./dist
```

#### 2. 契约验证失败
```bash
# 查看详细错误信息
npm run contract:validate --level strict

# 检查契约文件格式
cat ./dist/contract.yaml
```

#### 3. 文档生成失败
```bash
# 检查输出目录
mkdir -p ./docs/contract

# 检查契约文件是否存在
ls -la ./dist/contract.yaml
```

### 调试技巧

#### 1. 启用详细日志
```bash
# 设置日志级别
DEBUG=contract:* npm run contract:generate
```

#### 2. 检查依赖版本
```bash
npm list commander yaml
```

#### 3. 验证TypeScript配置
```bash
npx tsc --noEmit
```

## 📊 性能优化

### 1. 生成性能
- **减少源码分析范围** - 只分析必要的目录
- **缓存分析结果** - 缓存源码分析结果
- **并行处理** - 并行处理多个文件

### 2. 验证性能
- **增量验证** - 只验证变更的部分
- **规则优化** - 优化验证规则
- **并行验证** - 并行执行验证

### 3. 内存优化
- **流式处理** - 使用流式处理大文件
- **垃圾回收** - 及时释放内存
- **缓存管理** - 合理使用缓存

## 🔒 安全考虑

### 1. 敏感信息
- **避免敏感信息** - 不要在契约中包含敏感信息
- **访问控制** - 限制契约文件的访问权限
- **加密存储** - 加密存储敏感的契约信息

### 2. 输入验证
- **验证输入** - 验证所有输入参数
- **限制大小** - 限制契约文件大小
- **格式检查** - 检查文件格式

### 3. 错误处理
- **安全错误** - 不要暴露系统内部错误
- **日志记录** - 记录安全相关事件
- **监控告警** - 设置安全监控告警

## 📈 监控和指标

### 1. 关键指标
- **生成时间** - 契约生成耗时
- **验证分数** - 契约验证分数
- **错误率** - 契约管理错误率
- **文档更新频率** - 文档更新频率

### 2. 监控工具
- **应用监控** - 使用APM工具监控应用性能
- **日志分析** - 分析契约管理日志
- **指标收集** - 收集关键业务指标

### 3. 告警设置
- **生成失败** - 契约生成失败告警
- **验证失败** - 契约验证失败告警
- **性能下降** - 性能下降告警

## 🔄 版本管理

### 1. 语义化版本
- **主版本号** - 破坏性变更
- **次版本号** - 新功能
- **修订号** - 错误修复

### 2. 兼容性策略
- **向后兼容** - 保持向后兼容性
- **废弃策略** - 明确的废弃策略
- **迁移指南** - 提供迁移指南

### 3. 版本发布
- **版本标记** - 使用Git标签标记版本
- **发布说明** - 提供详细的发布说明
- **变更日志** - 维护变更日志

## 🎯 高级功能

### 1. 自定义验证规则
```javascript
// 自定义验证规则示例
const customRules = [
  {
    id: 'custom-rule',
    name: 'Custom Rule',
    description: 'Custom validation rule',
    type: 'custom',
    severity: 'warning',
    enabled: true,
    validator: (contract) => {
      // 自定义验证逻辑
      return { valid: true, message: 'Valid' };
    }
  }
];
```

### 2. 插件系统
```javascript
// 自定义插件示例
const customPlugin = {
  name: 'custom-plugin',
  hooks: {
    beforeGenerate: (config) => {
      // 生成前处理
    },
    afterGenerate: (result) => {
      // 生成后处理
    }
  }
};
```

### 3. 模板系统
```yaml
# 自定义模板示例
customTemplate:
  name: 'Custom API Template'
  version: '1.0.0'
  endpoints:
    - path: '/api/custom'
      method: 'GET'
      summary: 'Custom endpoint'
```

## 📚 扩展阅读

### 1. 官方文档
- [OpenAPI 3.0 规范](https://swagger.io/specification/)
- [TypeScript 文档](https://www.typescriptlang.org/docs/)
- [Node.js 文档](https://nodejs.org/docs/)

### 2. 相关工具
- [Swagger Editor](https://editor.swagger.io/)
- [Postman](https://www.postman.com/)
- [Insomnia](https://insomnia.rest/)

### 3. 学习资源
- [REST API 设计指南](https://restfulapi.net/)
- [API 文档最佳实践](https://www.moesif.com/blog/api-documentation-best-practices/)
- [微服务架构](https://microservices.io/)

## 🤝 贡献指南

### 1. 开发环境设置
```bash
# 克隆仓库
git clone <repository-url>

# 安装依赖
npm install

# 运行测试
npm test

# 启动开发服务器
npm run dev
```

### 2. 代码规范
- **TypeScript** - 使用TypeScript编写代码
- **ESLint** - 使用ESLint检查代码质量
- **Prettier** - 使用Prettier格式化代码
- **测试覆盖** - 保持高测试覆盖率

### 3. 提交规范
- **提交信息** - 使用清晰的提交信息
- **分支管理** - 使用Git Flow分支管理
- **代码审查** - 所有代码需要经过审查

### 4. 问题报告
- **Bug报告** - 使用GitHub Issues报告bug
- **功能请求** - 提出新的功能请求
- **文档改进** - 改进文档和示例

---

**文档版本**: 2.0.0  
**最后更新**: 2025-08-06  
**维护团队**: DSGS架构团队