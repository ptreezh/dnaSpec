# DNASPEC iflow 测试工具包

## 📦 包含内容

本项目已配置完整的 DNASPEC iflow 命令测试工具。

### 📁 测试文件

1. **quick_test.bat** - 交互式测试菜单（推荐使用）
2. **test_iflow_commands.bat** - 完整测试脚本
3. **DNASPEC_IFLOW_COMMANDS.md** - 完整使用文档
4. **test_iflow_dnaspec.md** - 测试指南

### ⚙️ 已安装的命令

项目中已安装 8 个 DNASPEC 命令，位于 `.iflow\commands\` 目录：

| 命令 | 功能 | 文件 |
|------|------|------|
| `/dnaspec.task-decomposer` | 任务分解 | dnaspec-task-decomposer.md |
| `/dnaspec.architect` | 系统架构设计 | dnaspec-architect.md |
| `/dnaspec.agent-creator` | 智能代理创建 | dnaspec-agent-creator.md |
| `/dnaspec.constraint-generator` | 约束规则生成 | dnaspec-constraint-generator.md |
| `/dnaspec.modulizer` | 代码模块化 | dnaspec-modulizer.md |
| `/dnaspec.dapi-checker` | API接口验证 | dnaspec-dapi-checker.md |
| `/dnaspec.cache-manager` | 缓存策略优化 | dnaspec-cache-manager.md |
| `/dnaspec.git-operations` | Git仓库管理 | dnaspec-git-operations.md |

---

## 🚀 快速开始

### 方式 1: 使用交互式测试工具（推荐）

双击运行或在命令行执行：
```cmd
quick_test.bat
```

然后按照菜单提示选择要测试的命令。

### 方式 2: 直接使用 iflow 命令

```cmd
cd D:\DAIP\dnaSpec

# 列出所有命令
iflow -p "列出所有 /dnaspec 开头的命令"

# 测试任务分解
iflow -p "/dnaspec.task-decomposer 将开发一个TODO应用分解为步骤"

# 测试架构设计
iflow -p "/dnaspec.architect 为博客系统设计架构"

# 进入交互模式
iflow
```

### 方式 3: 使用完整测试脚本

```cmd
test_iflow_commands.bat
```

---

## 📋 测试场景示例

### 1. 项目规划
```cmd
iflow -p "/dnaspec.task-decomposer 将开发一个电商网站分解为开发任务"
```

### 2. 架构设计
```cmd
iflow -p "/dnaspec.architect 为在线教育平台设计微服务架构"
```

### 3. 安全约束
```cmd
iflow -p "/dnaspec.constraint-generator 为支付API生成安全约束"
```

### 4. 代理创建
```cmd
iflow -p "/dnaspec.agent-creator 创建一个代码审查自动化代理"
```

### 5. 代码重构
```cmd
iflow -p "/dnaspec.modulizer 将单体应用重构为模块化架构"
```

---

## 🛠️ 故障排查

### iflow 命令未找到

**方案 1**: 使用完整路径
```cmd
node "C:\Users\Zhang\AppData\Roaming\npm\node_modules\@iflow-ai\iflow-cli\bundle\iflow.js" -p "测试命令"
```

**方案 2**: 确保 iflow 已全局安装
```cmd
npm list -g @iflow-ai/iflow-cli
```

### 命令无法识别

**检查步骤**:
1. 确保在项目根目录 `D:\DAIP\dnaSpec`
2. 检查 `.iflow\commands\` 目录是否存在
3. 验证命令文件是否完整

```cmd
# 检查命令文件
dir .iflow\commands\dnaspec-*.md
```

### 测试脚本运行失败

1. 以管理员身份运行命令提示符
2. 确保 PowerShell 执行策略允许运行脚本
3. 直接使用 iflow 命令而不依赖脚本

---

## 📚 更多资源

- **详细文档**: 查看 `DNASPEC_IFLOW_COMMANDS.md`
- **测试指南**: 查看 `test_iflow_dnaspec.md`
- **命令定义**: 查看 `.iflow\commands\` 目录中的文件

---

## 🎯 推荐测试流程

1. **验证安装**
   ```cmd
   quick_test.bat
   选择 [9] 列出所有命令
   ```

2. **简单测试**
   ```cmd
   选择 [1] 任务分解工具
   ```

3. **功能测试**
   ```cmd
   依次测试各个命令功能
   ```

4. **深度测试**
   ```cmd
   选择 [0] 启动交互模式
   ```

---

## ✅ 验证清单

使用此清单验证 DNASPEC iflow 集成是否正常：

- [ ] iflow 命令可用
- [ ] 项目目录正确 (`D:\DAIP\dnaSpec`)
- [ ] `.iflow\commands\` 目录存在
- [ ] 8个命令文件完整
- [ ] 能成功列出所有 dnaspec 命令
- [ ] 至少测试过 3 个不同命令
- [ ] 交互模式可以正常使用

---

## 📞 支持

如遇问题，请检查：
1. iflow 版本是否最新
2. Node.js 版本是否兼容
3. 项目文件是否完整
4. 命令文件格式是否正确

---

**生成时间**: 2025-12-25
**版本**: 1.0
**项目**: DNASPEC iflow Integration
